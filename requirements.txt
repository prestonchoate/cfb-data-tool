# --- Core engine (Phase 1) ---
# OCR: ONNX-based, no PyTorch (keeps install ~250MB vs ~2GB for EasyOCR)
rapidocr-onnxruntime
opencv-python
numpy

# Screen capture & global hotkey (used from Phase 2 onward)
mss
keyboard

# OS-appropriate config/data directories
platformdirs

# --- UI (Phase 2+) ---
PySide6

# --- Linux Wayland (optional) ---
# jeepney>=0.8
# pipewire-capture>=0.2.9
